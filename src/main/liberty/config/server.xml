<server description="Sample Servlet server">
    <featureManager>
        <feature>servlet-4.0</feature>
    </featureManager>

    <!-- <httpEndpoint httpPort="${default.http.port}" httpsPort="${default.https.port}" id="defaultHttpEndpoint"  host="*" /> -->
    <httpEndpoint httpPort="9080" httpsPort="9443" id="defaultHttpEndpoint"  host="*" />

    <!-- <webApplication id="spo_ui" location="spo_ui.war" contextRoot="${app.context.root}" /> -->
    <webApplication id="spo_ui" location="spo_ui.war" name="spo_ui" contextRoot="/ibm/spo_ui" />

</server>
